using System.ComponentModel;

namespace Core.Enum
{
    public enum PerfilUsuario
    {
        [Description("Usuario")]
        Usuario = 1,
        [Description("Administrador")]
        Administrador = 2
    }
}
